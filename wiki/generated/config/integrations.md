# integrations.yml

The table is generated from the current packaged YAML, so defaults stay synchronized with the source.

**Source:** [`src/main/resources/integrations.yml`](https://github.com/Sfg246/RelicPRisonDocs/blob/main/snapshots/resources/integrations.yml)

**Documented leaves:** 23

| Setting | Type | Packaged default | Recommended starting point | What it changes | Risk / common mistake |
| --- | --- | --- | --- | --- | --- |
| `file-version` | number | `1` | Keep the packaged default for the first successful staging run; tune only with a specific reason. | Configures `file-version` for this section of integrations.yml. | Wrong type or unsupported value can fail validation/reload or change gameplay unexpectedly. |
| `vault.enabled` | boolean | `true` | Begin with the packaged default, then enable features one at a time on staging. | Turns this feature or behavior on or off. | Enabling a dependency-backed feature before its dependency is ready can fail validation or behavior checks. |
| `vault.require-economy-provider` | boolean | `true` | Keep the packaged default for the first successful staging run; tune only with a specific reason. | Configures `require-economy-provider` for this section of integrations.yml. | Wrong type or unsupported value can fail validation/reload or change gameplay unexpectedly. |
| `luckperms.enabled` | boolean | `true` | Begin with the packaged default, then enable features one at a time on staging. | Turns this feature or behavior on or off. | Enabling a dependency-backed feature before its dependency is ready can fail validation or behavior checks. |
| `worldguard.enabled` | boolean | `true` | Begin with the packaged default, then enable features one at a time on staging. | Turns this feature or behavior on or off. | Enabling a dependency-backed feature before its dependency is ready can fail validation or behavior checks. |
| `worldguard.region-prefix` | string | `relicmine_` | Keep the packaged default for the first successful staging run; tune only with a specific reason. | Configures `region-prefix` for this section of integrations.yml. | Wrong type or unsupported value can fail validation/reload or change gameplay unexpectedly. |
| `worldguard.create-regions` | boolean | `true` | Keep the packaged default for the first successful staging run; tune only with a specific reason. | Configures `create-regions` for this section of integrations.yml. | Wrong type or unsupported value can fail validation/reload or change gameplay unexpectedly. |
| `worldguard.update-regions` | boolean | `true` | Keep the packaged default for the first successful staging run; tune only with a specific reason. | Configures `update-regions` for this section of integrations.yml. | Wrong type or unsupported value can fail validation/reload or change gameplay unexpectedly. |
| `worldguard.delete-regions` | boolean | `true` | Keep the packaged default for the first successful staging run; tune only with a specific reason. | Configures `delete-regions` for this section of integrations.yml. | Wrong type or unsupported value can fail validation/reload or change gameplay unexpectedly. |
| `worldguard.flags.deny-block-place` | boolean | `true` | Keep the packaged default for the first successful staging run; tune only with a specific reason. | Configures `deny-block-place` for this section of integrations.yml. | Wrong type or unsupported value can fail validation/reload or change gameplay unexpectedly. |
| `worldguard.flags.allow-block-break` | boolean | `true` | Keep the packaged default for the first successful staging run; tune only with a specific reason. | Configures `allow-block-break` for this section of integrations.yml. | Wrong type or unsupported value can fail validation/reload or change gameplay unexpectedly. |
| `worldguard.flags.deny-explosions` | boolean | `true` | Keep the packaged default for the first successful staging run; tune only with a specific reason. | Configures `deny-explosions` for this section of integrations.yml. | Wrong type or unsupported value can fail validation/reload or change gameplay unexpectedly. |
| `worldguard.flags.deny-fire-spread` | boolean | `true` | Keep the packaged default for the first successful staging run; tune only with a specific reason. | Configures `deny-fire-spread` for this section of integrations.yml. | Wrong type or unsupported value can fail validation/reload or change gameplay unexpectedly. |
| `worldguard.flags.deny-fluid-flow` | boolean | `true` | Keep the packaged default for the first successful staging run; tune only with a specific reason. | Configures `deny-fluid-flow` for this section of integrations.yml. | Wrong type or unsupported value can fail validation/reload or change gameplay unexpectedly. |
| `worldedit-fawe.enabled` | boolean | `true` | Begin with the packaged default, then enable features one at a time on staging. | Turns this feature or behavior on or off. | Enabling a dependency-backed feature before its dependency is ready can fail validation or behavior checks. |
| `itemsadder.enabled` | boolean | `false` | false until ItemsAdder behavior is verified on your staging server. | Turns this feature or behavior on or off. | Enabling it without ItemsAdder or valid custom IDs can make custom content unavailable. |
| `advanced-enchantments.enabled` | boolean | `false` | false until AdvancedEnchantments is installed and its supported mining behavior is staged. | Turns this feature or behavior on or off. | Bulk-mining integrations can affect drops, durability, fortune, and replay behavior. |
| `placeholderapi.enabled` | boolean | `true` | Begin with the packaged default, then enable features one at a time on staging. | Turns this feature or behavior on or off. | Enabling a dependency-backed feature before its dependency is ready can fail validation or behavior checks. |
| `geyser-floodgate.awareness-enabled` | boolean | `true` | Begin with the packaged default, then enable features one at a time on staging. | Turns this feature or behavior on or off. | Enabling a dependency-backed feature before its dependency is ready can fail validation or behavior checks. |
| `combat.provider` | string | `none` | none unless you have configured a supported combat-tag provider. | Configures `provider` for this section of integrations.yml. | An incorrect provider can change teleport restrictions unexpectedly. |
| `combat.teleport-restriction.enabled` | boolean | `false` | Begin with the packaged default, then enable features one at a time on staging. | Turns this feature or behavior on or off. | Enabling a dependency-backed feature before its dependency is ready can fail validation or behavior checks. |
| `combat.teleport-restriction.bypass-permission` | string | `relicprison.bypass.combat-teleport` | Keep the packaged default for the first successful staging run; tune only with a specific reason. | Names the permission node checked for the related action. | Wrong type or unsupported value can fail validation/reload or change gameplay unexpectedly. |
| `combat.teleport-restriction.deny-message` | string | `&cYou cannot teleport to mines while combat-tagged.` | Keep the packaged default for the first successful staging run; tune only with a specific reason. | Controls player-facing text for this part of the plugin. | Wrong type or unsupported value can fail validation/reload or change gameplay unexpectedly. |

::: tip Safe editing loop
Back up first, change one idea at a time, reload only supported modules, then run `/rp validate` and `/rp diagnose`. Use a full restart for storage/backend changes or anything the plugin documents as startup-only.
:::
